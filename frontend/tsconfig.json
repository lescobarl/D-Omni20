{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    // `incremental` con `noEmit` acelera el typecheck repetido (pre-commit/push):
    // solo re-tipifica lo cambiado entre corridas. El buildinfo va a la caché de
    // node_modules (ignorada por git) para no ensuciar el árbol.
    "incremental": true,
    "tsBuildInfoFile": "./node_modules/.cache/tsconfig.app.tsbuildinfo",

    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",

    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    // Sin `baseUrl`: desde TS 4.1 `paths` se resuelve relativo al tsconfig
    // (evita la deprecación de `baseUrl` que dejará de funcionar en TS 7.0).
    "paths": {
      "@/*": ["./src/*"]
    },

    "types": ["vite/client", "vitest/globals", "@testing-library/jest-dom"]
  },
  "include": ["src"]
}

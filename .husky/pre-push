#!/usr/bin/env sh
# OmniBotIA Studio - pre-push (verificación ligera local)
# La suite completa (`test:full`) la ejecuta el CI (GitHub Actions) en cada push.
# Aquí solo corre la puerta de tipos: el push local es casi instantáneo y el gate
# real de entrega (suite + backend + validadores) queda en remoto.
cd "$(dirname "$0")/../frontend" || exit 1
npm run typecheck
